{
  "name": "linguee-anki-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension to extract vocabulary from Linguee and add to Anki",
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js chrome",
    "build:firefox": "node build.js firefox",
    "icons": "node build.js --icons"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "sharp": "^0.35.1"
  }
}
